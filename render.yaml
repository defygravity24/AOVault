services:
  - type: web
    name: aovault
    runtime: node
    buildCommand: bash build.sh
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
